on: push
name: Deploy Website on push
jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Get latest code
      uses: actions/checkout@v2

    - name: sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with: 
        server: ftp.kushmaartproject.com.np
        username: kushmaar
        password: ${{ 8038@Zenith }}